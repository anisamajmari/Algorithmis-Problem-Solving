const db = require('../data');
/// data in this db is normalized, when we show info to user we need 
/// data as denormalized as possible denormalize as follows.



//// replace all id with real objects, 
//// project should introduce new tasks key where task objects will be there 
//// task should add 2 keys, logs and updates where respective codes will be placed there. 
