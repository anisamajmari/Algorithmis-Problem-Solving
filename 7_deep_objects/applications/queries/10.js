const {applications} = require("../data/applications");
const {companies} = require('../data/companies');
const {professions} = require('../data/professions');


/// find out which is the most occured skill in all applications